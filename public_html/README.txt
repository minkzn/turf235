TurfOps static site

Open index.html directly in a browser or publish the entire folder to GitHub Pages / Netlify.

Files:
  index.html
  assets/styles.css

assets/hero-golf.jpg, turf.jpg, stadium.jpg, and lion.jpg are unused —
the page currently uses CSS gradient panels instead (those files had
mockup text baked into the images, which caused stretching/duplicate
text once dropped into the real layout). Swap in plain, text-free
photos and point the CSS at them whenever you have real photography.

Note: This is a personal educational concept, not an official Penn State site or program.
